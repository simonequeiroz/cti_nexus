{
  "name": "projeto-cti-nexus",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend-vue",
    "build": "npm run build --prefix frontend-vue",
    "preview": "npm run preview --prefix frontend-vue"
  }
}

